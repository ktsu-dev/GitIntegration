.NET;C#;git integration;git;git wrapper;version control;source control;fluent api;git status;git log;git diff;git provider;github;octokit;semantic types;strongly typed;branch name;commit sha;credentials;credential cache;dependency injection;dotnet;csharp;git cli;process runner;command builder;git init;git clone;git commit;git add;git checkout;git branch;git remote